{
  "name": "@endbug/jest-pr-reporter",
  "version": "0.0.1",
  "description": "An opinionated Jest reporter meant for GitHub Actions & PRs",
  "repository": "https://github.com/EndBug/jest-pr-reporter",
  "author": "Federico Grandi",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "main": "dist",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "prepare": "npm run build",
    "test": "jest"
  },
  "dependencies": {
    "@actions/github": "^6.0.1",
    "@jest/reporters": "^30.0.5",
    "@octokit/graphql-schema": "^15.26.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "22.x.x",
    "jest": "^30.0.5",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}
