{
  "name": "jest-reporter-template",
  "version": "0.0.1",
  "description": "An opinionated Jest reporter meant for GitHub Actions & PRs",
  "repository": "https://github.com/EndBug/jest-reporter-template",
  "author": "Federico Grandi",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "main": "dist",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "test": "jest"
  },
  "dependencies": {
    "@jest/reporters": "^30.0.5"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "22.x.x",
    "jest": "^30.0.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}
